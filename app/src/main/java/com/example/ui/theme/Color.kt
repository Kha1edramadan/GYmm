package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ── Base surfaces ─────────────────────────────────────────────────────────────
val Surface                 = Color(0xFF111313)
val SurfaceContainerLowest  = Color(0xFF0C0E0E)
val SurfaceContainerLow     = Color(0xFF141616)
val SurfaceContainer        = Color(0xFF191B1B)
val SurfaceContainerHigh    = Color(0xFF222424)
val SurfaceContainerHighest = Color(0xFF2C2E2E)
val SurfaceVariant          = Color(0xFF2C2E2E)

// ── Text ──────────────────────────────────────────────────────────────────────
val OnSurface        = Color(0xFFE8E7E7)
val OnSurfaceVariant = Color(0xFF5A5959)

// ── Brand / accent ────────────────────────────────────────────────────────────
val Primary   = Color(0xFFC3F400)   // neon lime — main accent
val OnPrimary = Color(0xFF131A00)

// ── Semantic extras ───────────────────────────────────────────────────────────
val Positive  = Color(0xFF34D399)   // green — good / active
val Warning   = Color(0xFFFFB347)   // amber — warning
val Danger    = Color(0xFFFF6B6B)   // red   — error / over-budget
val InfoBlue  = Color(0xFF60A5FA)   // blue  — info

// ── Outline ───────────────────────────────────────────────────────────────────
val Outline        = Color(0xFF3A3C3C)
val OutlineVariant = Color(0xFF2A2C2C)

// ── Legacy compat ─────────────────────────────────────────────────────────────
val Error    = Color(0xFFFF6B6B)
val OnError  = Color(0xFF3B0000)
